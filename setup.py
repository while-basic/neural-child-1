from setuptools import setup, find_packages

setup(
    name="neural-child",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        "pip>=21.0",
        "setuptools>=65.0"
    ],
    python_requires=">=3.8",
)
